# 🗺️ PLAN.MD: Архитектура Личной Платформы

## 1. Структура Навигации (UX)

* **Главная (Home)**: Краткий оффер «Кто я и чем полезен» + кнопка-призыв к перехову к портфолио.
* **Портфолио (Portfolio)**: Вкладка в основном хедере. Состоит из «Главной статьи» (Rich Text) и сетки кейсов.
* **Блоги (Blogs)**: Сообщество. При переходе появляется **Sub-header** с топовыми тегами (#laravel, #react и т.д.).
* **Обо мне**: Ссылка в футере или хедере, ведущая на подробный лонгрид.

---

## 2. Система «Топовости» (Ranking System)

Чтобы определить, что «круто», внедрим простую, но чещную формулу:

* **Лайки (Сердечки)**: Создаем таблицу `likes`. Один пользователь — один лайк на пост.
* **Комментарии**: Вес комментария в рейтинге.
* **Механика**: В `ArticleService` добавим фильтры:
* `sort=top_likes` (по количеству лайков).
* `sort=top_comments` (по активности обсуждения).
* `sort=trending` (свежие посты с высокой активностью).



---

## 3. Техническая реализация (Back-to-Front)

### 🧱 Бэкенд (Laravel)

* [ ] **Auth**: Установка Laravel Breeze (API-версия).
* [ ] **Database**:
* [ ] `users`: Добавить поле `role` (admin/user) и `avatar`.
* [ ] `articles`: Добавить `user_id`, `type` (blog/portfolio), `is_main`.
* [ ] `likes`: Таблица связки `user_id` и `article_id`.
* [ ] `tags`: Таблица тегов и таблица-связка с артикулами.


* [ ] **API**:
* [ ] `GET /api/portfolio`: Возвращает главную статью и кейсы.
* [ ] `POST /api/articles/{id}/like`: Переключатель лайка.
* [ ] `GET /api/tags/top`: Список самых популярных тегов для Sub-header.



### 🎨 Фронтенд (React)

* [ ] **Routing**: Настройка `ViewState` в `App.tsx` для новых вкладок.
* [ ] **Components**:
* [ ] `PortfolioView`: Рендерит TipTap-контент + CaseGrid.
* [ ] `BlogSubHeader`: Панель с тегами, которая появляется только в режиме 'blogs'.
* [ ] `LikeButton`: Анимированное сердечко с неоновым свечением.


* [ ] **Admin Logic**:
* [ ] Условие в `ArticleForm`: кнопки медиа (фото/видео) доступны только для `user.role === 'admin'`.
* [ ] Запрет на редактирование чужих постов.



---

## 4. Маркетинговые фишки (Wow-эффект)

* [ ] **Glow-эффект**: Сердечко при клике «взрывается» синими частицами.
* [ ] **Video Support**: Интеграция YouTube/MP4 в TipTap специально для кейсов портфолио.
* [ ] **Smooth Transitions**: Плавная смена хедеров при переходе в блоги.
